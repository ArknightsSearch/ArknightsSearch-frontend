import TextData from './TextData.vue'
import CharExtra from './CharExtra.vue'
import RegexData from './RegexData.vue'

const extraDict = {
    text: TextData,
    char: CharExtra,
    regex: RegexData
}

export default extraDict
